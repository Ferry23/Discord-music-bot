discord.py>=2.0.0
yt-dlp>=2023.0.0
youtube-search-python>=1.6.0
lyricsgenius>=3.0.0
python-dotenv>=1.0.0
PyNaCl

# System dependencies (not installed via pip):
# - FFmpeg (required for audio playback)
#   Windows: Install via Chocolatey: choco install ffmpeg
#   Linux/Railway: Automatically installed via Nixpacks/apt
#   Or download from: https://ffmpeg.org/download.html